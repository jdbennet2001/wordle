apiVersion: v1
kind: Service
metadata:
  name: {{ include "node-wordle.fullname" . }}
  labels:
    {{- include "node-wordle.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "node-wordle.selectorLabels" . | nindent 4 }}
